<base:UserControlBase
    x:Class="NarakaBladepoint.Modules.Social.UI.Social.Views.SocialPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:attach="clr-namespace:NarakaBladepoint.Framework.UI.AttachedProperties;assembly=NarakaBladepoint.Framework"
    xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:base="clr-namespace:NarakaBladepoint.Framework.Core.Bases;assembly=NarakaBladepoint.Framework"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:NarakaBladepoint.Modules.Social.UI.Social.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="clr-namespace:NarakaBladepoint.Modules.Social.UI.Social.ViewModels"
    d:Background="Gray"
    d:DataContext="{d:DesignInstance Type=viewmodels:SocialPageViewModel}"
    d:DesignHeight="100"
    d:DesignWidth="300"
    mc:Ignorable="d">
    <UniformGrid Grid.Column="3" Columns="5">
        <Image attach:HoverHighlightAttachedProperty.EnableHoverHighlight="True" Style="{StaticResource Music}">
            <b:Interaction.Triggers>
                <b:EventTrigger EventName="MouseLeftButtonDown">
                    <b:InvokeCommandAction Command="{Binding NavigateToMusicCommand}" />
                </b:EventTrigger>
            </b:Interaction.Triggers>
        </Image>
        <Image attach:HoverHighlightAttachedProperty.EnableHoverHighlight="True" Style="{StaticResource Tutorial}">
            <b:Interaction.Triggers>
                <b:EventTrigger EventName="MouseLeftButtonDown">
                    <b:InvokeCommandAction Command="{Binding NavigateToTutorialCommand}" />
                </b:EventTrigger>
            </b:Interaction.Triggers>
        </Image>
        <Image attach:HoverHighlightAttachedProperty.EnableHoverHighlight="True" Style="{StaticResource Email}">
            <b:Interaction.Triggers>
                <b:EventTrigger EventName="MouseLeftButtonDown">
                    <b:InvokeCommandAction Command="{Binding NavigateToEmailCommand}" />
                </b:EventTrigger>
            </b:Interaction.Triggers>
        </Image>
        <Image attach:HoverHighlightAttachedProperty.EnableHoverHighlight="True" Style="{StaticResource Contact}">
            <b:Interaction.Triggers>
                <b:EventTrigger EventName="MouseLeftButtonDown">
                    <b:InvokeCommandAction Command="{Binding NavigateToFrendListCommand}" />
                </b:EventTrigger>
            </b:Interaction.Triggers>
        </Image>
        <Image attach:HoverHighlightAttachedProperty.EnableHoverHighlight="True" Style="{StaticResource Setting}">
            <b:Interaction.Triggers>
                <b:EventTrigger EventName="MouseLeftButtonDown">
                    <b:InvokeCommandAction Command="{Binding NavigateToSettingCommand}" />
                </b:EventTrigger>
            </b:Interaction.Triggers>
        </Image>
    </UniformGrid>
</base:UserControlBase>